<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/base_url_bg_color">
    <include layout="@layout/base_url_manager_toolbar"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>
    <EditText
        android:id="@+id/etUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:layout_marginTop="6dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:textSize="@dimen/base_url_edit_url"
        android:textColor="@color/base_url_edit_url_text_color"
        android:singleLine="true"
        android:hint="@string/base_url_hint_example_url"/>
    <Button
        android:id="@+id/btnAdd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="16dp"
        android:textAllCaps="false"
        android:textSize="@dimen/base_url_btn_add"
        android:text="@string/base_url_add"
        android:textColor="@color/base_url_btn_add_text_color"
        android:background="@drawable/base_url_btn_add_selector"
        android:onClick="onClick"
        style="?android:attr/borderlessButtonStyle"/>
</LinearLayout>